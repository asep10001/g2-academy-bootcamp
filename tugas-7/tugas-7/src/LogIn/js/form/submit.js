import React, { Component } from "react";

export class Submit extends Component {
  render() {
    return (
      <button
        type="submit"
        className="ui button"
        tabIndex="-1"
      >
        Login
      </button>
    );
  }
}
